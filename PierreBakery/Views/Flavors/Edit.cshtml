@{
  Layout = "_Layout";
}

<!-- @model Factory.Models.Machine

<div class = "container">
  <h2>Editing Machine: @Html.DisplayFor(model => model.Name)</h2>

  <div class = "infoBox">
    @using (Html.BeginForm())
    {
      @Html.HiddenFor(model => model.MachineId)

      <h3>Machine @Html.LabelFor(model => model.Name)
      @Html.EditorFor(model => model.Name)</h3>
      <br />
      <input type = "submit" value = "Save" class = "btn2" />
    }
    @Html.ActionLink("Cancel", "Index", null, null, new { @class = "btn" })
  </div>
</div>
 -->
